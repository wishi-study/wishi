새로운 노트
2026.05.17 일 오후 2:20 ・ 25분 4초
장진우


참석자 1 00:01
네 그러면 타임 스크립트 클래스와 관련해서 리드 온이 키워드는 무엇인지 설명해 주세요.

참석자 2 00:10
네 리드 올리는 이제 말 그대로 읽을 수 있는 타입인데 읽기만 가능하고 변경은 되지 않음을 보장하는 키워드입니다.
그래서 이 리드 월리 키워드는 보통 객체에서 어떤 속성을 정의할 때 이제 프라이빗 메서드와 함께 이 리드 올리를 선언해서 사용을 합니다.
타임 스크립트 클래스에서 오버라이딩을 어떻게 구현할 수 있나요?
네 오버라이딩 일단 개념은 이 부모에 있는 메서드를 이 자식이 재정의해서 사용을 하는 것인데 어 타임 스크립트에서는 이때 재정의할 메서드에 어 메서드 앞에 이제 오버라이드라는 키워드를 붙여서 사용할 수 있습니다.
추상 클래스란 무엇인지 설명해 주시겠어요? 네 추상 클래스는 추상 메서드를 가지고 있는 것을 이제 추상 클래스라고 하고 이러한 추상 메서드는 보통 이제 다른 클래스가 상속을 받을 때 그 내부의 구체적인 동작 방식을 정의해서 구체화하는 구체화합니다.

참석자 2 01:14
그래서 타임 스크립트에서 이제 추상 클래스를 구현을 하기 위해서는 먼저 클래스 자체에 앱스트랙트라는 키워드를 붙일 수 있습니다.
이 키워드를 붙이면 객체를 생성하는 것을 이제 막을 수 있습니다.
그리고 어 메서드를 정의할 때 이제 추석 메서드를 정의하고 싶으면 앱스트랙트라는 키워드를 붙이면 되고 그렇지 않고 모든 클래스가 공유하고 싶은 메서드에는 해당 키워드를 붙이지 않고 정의할 수 있습니다.
싱글톤 클래스가 뭔지 설명해 주세요. 싱글톤 클래스는 한 번만 생성되는 것을 보장하는 클래스입니다.
보통 싱글톤 클래스는 이제 여러 클래스들이 공유해서 사용하는 경우에 사용을 하고 내부의 상태 값을 가지지 않는 경우에 사용이 되는 클래스입니다.
어 타입 스크립트에서 이 싱글톤 클래스를 구현하는 방법은 먼저 이 클래스가 외부에서 무분별하게 생성이 되면 안 되기 때문에 생성자 자체를 일단 프라이빗으로 설정할 수 있는 방법이 있습니다.

참석자 2 02:21
그리고 스태틱 메서드를 정의해서 사용을 할 수 있는데 이때 내부적인 로직으로는 만약에 해당 클래스가 생성이 되어 있으면 그 해당 클래스를 반환을 하고 그렇지 않으면 새로 생성해서 반환하는 방식으로 내부 로직을 구현할 수 있습니다.

참석자 1 02:41
타임 스크립트의 제네릭이란 무엇인지

참석자 2 02:44
설명해 주시고 함수 클래스 타입 엘리어스의 예시를 들어주세요.
네 제네릭 타입은 타입 자체를 일반화한 개념이라고 설명할 수 있습니다.
이 제네릭 타입을 사용을 하는 이유는 어떤 특정 로직을 작성할 때 만약에 특정 변수가 여러 가지 타입을 가질 수 있는데 보통 이러한 부분을 구현할 때 각각의 로직을 이제 타입에 맞춰서 새롭게 작성을 하게 됩니다.
이럴 경우에 중복된 코드가 많이 작성을 하기 때문에 그냥 하나의 제네릭 타입으로 사용을 하면 이제 코드를 줄일 수 있다는 점에서 장점이 있어서 사용을 하고 함수나 이제 클래스 그리고 타입에서 이 제네릭을 사용할 때 이제 사용 방법이 크게 다르지는 않은데 이제 껍새에다가 이제 사용될 타입을 정의하고 내부적으로 그 타입을 꺽새의 제네릭을 사용을 하고 이제 내부적으로 그 제네릭 타입을 이제 사용하는 방식으로 구현을 할 수 있습니다.
파셜 유틸리티 타입에 대해서 설명해 주세요.

참석자 2 03:48
네 파셔은 일단 뜻 그 자체가 뭔가 부분적이라는 뜻을 가지고 있는데 이 파시얼 유틸리티 타입을 사용하면 객체를 새롭게 생성할 때 이제 원하는 그 속성들만 가지고 객체를 생성할 수 있도록 만드는 타입입니다.

참석자 1 04:06
리커드 유틸리티 타입은 어떤 건지 설명해 주세요.

참석자 2 04:10
네 리카드는 리카올드를 사용해서 이제 타입을 정리하면 객체를 생성할 때 그 내부에 있는 속성들을 반드시 구현을 해야 된다는 것을 의미하고 그 속성 자체가 만약에 선택적 매개 변수여도 이리카드를 사용하면 반드시 구현을 구현을 해야 됩니다.
그러면 레코드 유틸리티 타입은 뭔가요? 레코드는 이제 객체를 생성할 때 사용할 수 있는 타입인데 이때 속성과 어떤 값을 특정 타입으로 강제해서 지정을 하고 싶을 때 레코드라는 타입을 사용할 수 있고 이걸 사용할 때는 그 꺽새 안에 어 키와 그리고 밸류 부분에 어떤 타입을 사용할지를 지정해서 사용할 수 있습니다.
오미 유틸리티 타입은 무엇인가요? 네 오밋은 생략한다는 뜻인데요.
이 오 유틸리티 타입을 사용을 하게 되면 객체를 생성할 때 이제 특정 속성을 제거하고 객체를 생성할 수 있습니다.

참석자 2 05:16
그래서 보통 이 오미 유틸리티 타입이 이제 사용이 되는 경우는 예를 들어서 이제 유저라는 객체가 있는데 이 유저의 객체에 특정 값인 예를 들어서 패스워드 값들은 보통 같이 전달을 안 해주기 때문에 이러한 값들을 속성을 빼고 생성하는 경우에 오미 유틸리티 타입을 생성할 수 있습니다.
그러면 픽 유틸리티 타입에 대해 설명해 주세요. 네 픽 유틸리티 타입은 특정한 속성들을 이제 선택을 해서 그것들로만 객체를 생성하고 싶을 때 사용할 수 있는 타입입니다.
그래서 이게 사용이 되는 경우는 이제 보통 이제 도메인의 유저 객체에서 이 값을 DTO로 변환하고 싶을 때 이제 특정 속성들만 가지고 DTO를 생성할 때 이 픽 유틸리티 타입을 사용할 수 있을 것 같습니다.
익스컬로드

참석자 1 06:11
유틸리티 타입은 무엇인가요?

참석자 2 06:13
네 익스클루드는 유니언 타입을 사용할 때 사용할 수 있는 타입인데 그 유니온 타입으로 정의되어 있는 여러 가지 타입들 중에서 특정 타입을 제거하고 정의하고 싶을 때 사용할 수 있습니다.
그럼 리드 올리 유틸리티 타입은 무엇인가요? 리드 올리 유틸리티 타입은 객체를 생성할 때 이 객체를 읽기만 가능하고 그 내부에 있는 속성 값을 변경할 수 없음을 의미 나타내기 위해서 사용할 수 있는 타입입니다.
널리실 병합이란 무엇인가요? 널리시 병합은 어떤 값이 널이나 언디파인드라는 값을 가질 수 있을 때 이 값 자체로 반환하는 게 아니라 어떤 특정 값으로 이제 변환해서 사용하고 싶을 때 널리시 병합을 사용할 수 있고 이때 물음표 물음표라는 키워드를 사용해서 그 반환 값에 대해서 설정할 수 있습니다.
레디스 리스트에 bl 팝 b알파 이랑 일반 알파의 차이에 대해서 설명해 주세요.
네 일단 LP과 이제 bl 팝 그 차이를 말씀을 드리면 첫 번째로는 이제 블로킹 여부에 대해서 차이가 있을 것 같습니다.

참석자 2 07:38
그래서 엘 팝 같은 경우는 리스트에 값이 존재하지 않으면 이제 단순히 늘 값을 반환을 하거나 아니면 값이 존재할 때까지 계속해서 폴링해서 값을 조회하는 방식으로 동작을 합니다.
반면에 이제 bl 팝은 이제 블로킹이 추가된 팝 기능으로 어 데이터가 존재하지 않는다면 이제 특정 시간 동안 이 블로킹 된 상태로 데이터를 조회하기 때문에 폴링 방식보다는 이제 리소스를 더 적게 사용한다는 점에서 장점이 있습니다.
그리고 데이터 값을 반환하는 것의 차이 개수에 차이가 있을 것 같습니다.
그래서 LP 같은 경우는 팝된 데이터 그 하나만을 반환하는 데 반해서 bl 팝 같은 경우는 여러 개의 리스트에서 이 대기를 해서 여러 리스트에서 이제 대기하면서 팝을 기다릴 수 있기 때문에 어떤 리스트에서 값을 팝했는지에 대한 그 리스트 정보 그리고 팝 데이터 이렇게 두 가지 값을 반환한다는 차이점이 있습니다.

참석자 1 08:45
그럼 레디스 스트림에 컨슬러 그룹이란 무엇이고 어떻게 동작하는지 설명해 주세요.

참석자 2 08:52
일단 그 소비자 그룹이라는 것은 레디스에서 스트림에 있는 데이터를 읽는 그 주체를 컨슈머라고 하는데 그 컨슈머들의 그룹을 컨슈머 그룹이라고 합니다.
그리고 특징으로는 데이터를 중복해서 읽을 수 없다라는 특징을 가지고 있고 가지고 있습니다.
그리고 여러 개의 소비자 그룹에 대해서는 이제 독립적으로 동작을 하는데 만약에 첫 번째 소비자 그룹이 어떤 데이터를 읽었을 때 그 소비자 그룹 내에서는 중복해서 값을 읽을 순 없지만 만약에 중복해서 값을 읽을 수 없지만 다른 소비자 그룹에서는 중복해서 값을 읽을 수 있다라는 특징을 가지고 있습니다.

참석자 2 09:44
그러면 어떤 상황에서 소비자 그룹을 사용하는 게 좋을까요?
소비자 그룹에서는 데이터를 이제 중복해서 읽지 않기 때문에 분산 처리하는 상황에서 이제 사용하면 좀 더 빠르게 처리할 수 있을 것 같습니다.

참석자 1 10:04
그러면 레디스 스트림에서 컨스너가 메시지를 처리하던 과정 중에서 장애가 발생하면 어떻게

참석자 2 10:10
구할 수 있을까요?

참석자 2 10:15
장애가 발생 그러니까 일단 레디스에서는 이 데이터 정합성을 위해서 이제 보류 리스트라는 것을 활용을 하는데, 만약에 장애가 발생해서 그 보류 리스트의 값이 남아 있게 되면 특정 시간 동안 이 값이 남아 있게 되면 이제 다른 소비자가 이 값을 처리할 수 있도록 하는 방식이 있습니다.
그래서 이 방식은 클레임이라는 명령어를 사용해서 특정 소비자가 이제 처리할 수 있게 지정하는 방식이 있습니다.
근데 이렇게 매번 지정하는 방식은 좀 불편하기 때문에 자동으로 처리할 수 있게 하는 방법이 있고 이것은 오토 클레임이라는 명령어로 사용을 할 수 있습니다.
그리고 만약에 계속해서 다시 시도를 하는데 특정 카운트 값 이상으로 실행이 되게 되면 이때는 그 데드레터 큐라는 곳에 데이터가 보내지게 되는데 이때는 이제 개발자가 수동으로 값을 처리하도록 구현하는 방법도 있습니다.

참석자 1 11:23
그럼 메디스 스트림에서 앤 모스트 원스 앤 리스트 원스, 이그제크리 원스의 차이를 설명해 주세요.

참석자 2 11:32
네 첫 번째로 엣 모스트 원스는 데이터가 최대 한 번 처리된다라는 의미입니다.
그래서 처리가 될 수도 있고 되지 않을 수도 있다라는 뜻인데 처리가 되지 않는 경우가 않는 경우에는 처리가 되지 않는 경우에는 소비자가 데이터를 받았을 때 이제 바로 앱 응답을 보내게 되는데 이때 앱 응답을 보냈지만 소비자가 처리하는 과정에서 데이터가 유실되게 된 경우에 이제 처리하지 않았다라는 것을 의미합니다.
그래서 이 방식의 장점은 응답을 빨리 보내기 때문에 속도 측면에서의 장점이 있지만 데이터가 유실될 수 있다는 단점을 가지고 있습니다.
그리고 두 번째로 리스트 리스트 원스 방식은 최소 한 번 처리된다라는 의미로 데이터가 중복해서 처리될 수 있음을 의미합니다.
그래서 이 방식 같은 경우는 아까와는 다르게 소비자가 데이터를 제대로 처리를 한 다음에 액을 응답하는 방식입니다.

참석자 2 12:41
하지만 이제 앱 응답을 보냈지만 이 값이 지연되는 경우에 이제 레디스 스트림 쪽에서 다시 한 번 데이터를 보낼 수 있고 이 경우에 이제 중복해서 데이터가 처리되기 때문에 이 부분을 유의해서 개발을 진행해야 됩니다.
그리고 세 번째로 이그세클리 원스라는 타입은 정확히 딱 한 번 처리되는 것을 의미합니다.
그래서 이때는 어 이제 레디스의 셋 같은 자료 구조를 사용해서 이제 중복 값을 저장하지 않도록 이 소비자 측에 셋 자료 구조를 사용해 구현하는 방식으로 최대한 방식으로 한 번만 처리됨을 이제 강제할 수 있습니다.
방금 에크 커맨드를 말씀해 주셨는데 그거 뭔지 조금 더 자세하게 설명을 해 주시고 또 보류 리스트에 대해서 알고 계신 게 있다면 설명해 주세요.
네 애기라는 것은 메시지를 소비자가 받았을 때 그 메시지를 잘 처리했음을 이제 스트링 쪽으로 알려주기 위한 응답 값을 액이라고 합니다.

참석자 2 13:46
그리고 이제 보류 리스트라는 개념은 레디스 스트림에서 이제 소비자가 데이터를 잘 처리했는지에 대한 정확성을 보장하기 위해서 사용하는 개념인데 어 소비자가 데이터를 가지고 갔을 때 그때 보류 리스트에 그 해당 데이터 값을 먼저 추가하게 됩니다.
그리고 소비자가 잘 처리했다고 앱 요청을 보내 액 응답 값을 보내게 되면 이때 보류 리스트에서 해당 데이터를 삭제해서 이 데이터가 잘 처리되었음을 나타낼 수 있습니다.

참석자 2 14:27
레디스 스트림이랑 카프카의 차이점에 대해서 말씀해 주세요.
네 일단 가장 큰 차이점은 이제 레디스는 인메모리 기반으로 동작을 한다는 점이고 카프카는 이제 디스크 기반으로 동작한다는 차이점이 있습니다.
그리고 데이터를 저장하는 방식에도 차이점이 있습니다.
카프카는 이제 토픽이라는 단위로 데이터를 저장하고 그 안에 파티션으로 이제 구분 지어서 데이터를 저장하는데 데이터 자체는 이제 파티션 내에서 이제 순서랑 그리고 유일성을 보장할 수 있습니다.
반면에 레디슨은 이 파티션이라는 개념이 없고 스트림 그 자체의 내부에서 이제 데이터의 순서와 유일성을 보장할 수 있습니다.
이로 인해서 또 소비자 그룹의 개념에도 차이가 있습니다.
카프카 같은 경우는 이제 소비자 그룹에서 하나의 소비자가 이제 파티션에 1대 1로 대응되는 반면에 레디스는 소비자 그룹 전체가 하나의 이제 스트림과 연결된다라는 차이점을 가지고 있습니다.

참석자 2 15:46
그러면 레디스에서 메시지 브로커를 구현하는 방법 퍼서 리스트 스트림을 비교해서 설명해 주세요.
먼저 가장 비교가 많이 되는 이 스트림과 팝섭을 먼저 비교해서 설명을 드리겠습니다.
어 일단 가장 큰 차이점은 데이터 저장하는 유무라고 생각을 합니다.
그래서 스트림은 데이터를 전달했을 때 그 값을 저장을 할 수가 있고 팝업은 저장하지 않는 방식입니다.
이로 인해서 데이터가 유실됐을 때의 처리에 있어서도 차이점이 있다고 생각을 하는데 스트림은 데이터를 저장하고 있기 때문에 다시 재처리하는 기능을 제공하고 있고 팝업은 이 기능을 제공하지 않습니다.
그리고 어 데이터를 전달할 때 팝업은 이 브로드캐스트 방식으로 데이터를 전달하기 때문에 여러 소비자가 중복된 데이터를 가질 수 있다라는 이제 차이점이 있는데 반면에 스트림은 소비자가 이제 하나의 소비자 그룹 내에서는 중복되지 않은 데이터를 갖는다라는 차이점도 있을 것 같습니다.

참석자 2 16:58
그리고 이제 리스트와 이 방식들에 대해서 비교해서 설명을 하면 일단 리스트는 푸시랑 팝이라는 굉장히 간단한 연산으로 데이터를 전송할 수 있는 자료 구조이기 때문에 사용하는 방법이 이제 다른 방식들에 비해서 굉장히 간단하다라는 특징을 가지고 있습니다.
또한 리스트도 이제 내부적으로 데이터를 저장하고 있기 때문에 장애가 발생했을 때 이제 복구할 수 있다라는 장점도 가지고 있습니다.
하지만 어 데이터가 유실됐을 때 이를 재처리하는 부분을 이 레디스 리스트 측에서 이제 제공을 해 주는 게 아니라 개발자가 직접 구현을 해야 되고 이 단순한 만큼 정교한 기능들은 이제 떨어지기 때문에 이 부분에서는 떨어지기 때문에 좀 더 정말 간단한 작업에서 리스트 자료 구조를 사용해야 된다고 생각합니다.
그러면 만약 실시간 알림 서비스를 구현해야 한다고 하면 진우 님은 어떤 방식을 선택할 것 같은지 또 이유도 궁금합니다.
잠시 생각하고 말씀드리겠습니다.

참석자 2 18:35
저는 리스트 방식을 사용할 것 같습니다. 알림 시스템이라고 한다면 그 동일한 알림을 이제 여러 똑같은 사람들에게 전송을 하는 어 방식일 텐데 이때 팝업과 그리고 이제 리스트라는 자료 구조가 일단 적합하다는 생각이 먼저 들었습니다.
그래서 그 두 개를 가지고 비교를 해 봤을 때 일단 리스트 방식이 좀 더 단순한 구현 방식이고 또 데이터를 저장하고 있기 때문에 문제가 발생했을 때 다시 재처리할 수 있다는 점에서 팝섭에 비해 장점을 더 많이 가지고 있다고 생각을 하고 이 알림 시스템 자체는 굉장히 단순하다고 생각이 들기 때문에 리스트 방식을 선택할 것 같습니다.

참석자 1 19:34
메시지 큐에서 프로듀서와 컨수머의 역할에 대해서 설명해 주세요.

참석자 2 19:44
프로듀션은 데이터를 생성해서 추가하는 주체이고 그리고 컨슈머는 그 데이터를 읽어서 처리하는 주체입니다.

참석자 1 20:02
그러면 이렇게 메시지 큐에서 생산자랑 소비자랑 나눠서 처리하는 이런 시스템이 생성된 이유가 뭘까요?

참석자 2 20:15
이렇게 나누는 것의 장점을 어떻게 생각하시는지 궁금합니다.
잠시 생각하고 말씀드리겠습니다.

참석자 2 20:50
네 일단 그런 식으로 나누어서 처리를 하는 방식은 뭔가 빗대어서 설명을 하자면 이제 동기적으로 처리하는 게 아니라 이제 비동기적으로 처리하는 방식이라고 생각이 됩니다.
그래서 응답 값을 그러니까 바로 처리하지 않아도 되는 이제 경우 그러니까 바로 응답 값을 반환을 하지 않아도 되는 경우에는 그 작업을 등록을 해두고 이제 응답 값을 반환하기 때문에 좀 더 속도적인 측면에서 빠르게 구현을 할 수 있어서 나뉘어졌다고 생각을 하고 대표적으로 저희가 회원 가입 같은 걸 했을 때 이제 메일 보내는 그 로직이 있는데 그럼 메일 보내는 로직은 이제 동기적으로 처리되지 않아도 되기 때문에 사용자에게는 빠른 응답 값을 보내주고 이후에 추 이후에 이제 데이터를 실행하는 방식으로 처리하면 이 방식이 좀 더 효율적이기 때문에 이 구조가 나왔다고 생각을 합니다.

참석자 2 21:54
꼬리 좀 더 드릴까요 아니면 그냥 레디스 스트림에 그 메시지 아이디가 어떻게 구성되어 있는지 아시는지 말씀해 주시고 또 카프카랑 어떤 차이가 있는지 말씀해 주세요.
카프카는 일단 데이터에서 카프카는 이제 파티션에서 데이터가 그 아이디 값이 1씩 증가하는 방식으로 이제 부여가 된다고 알고 있습니다.
그리고 레디스 스트림에서는 두 개 값으로 나뉘어서 그 아이디가 정의가 되는데 이제 첫 번째 부분에서는 데이터가 생성된 시각이 저장이 되고 그리고 두 번째는 이 동일한 시간에 여러 개의 데이터가 생성될 수 있기 때문에 그 데이터의 개수가 저장이 된다고 알고 있습니다.

참석자 1 23:09
그럼 만약에 레디스트린을 뭔가 활용한다면 어떤 시스템에서 활용을 하실 것 같으세요?

참석자 2 23:17
잠시 생각하고 답변드리겠습니다.

참석자 2 23:57
잘 모르겠습니다.

참석자 2 24:02
레디스 스트림에서 엑스 리드 커맨드랑 엑스 레인지 커맨드의 차이점을 알고 계신가요?
잠시 생각하고 말씀드리겠습니다.

참석자 2 24:32
그냥 단순한 그냥 차이점 정도만 알고 있는데 엑스 리드는 그냥 하나의 데이터를 조회하는 방식이고 그다음에 레인지는 범위에 대해서 조회하는 명령어의 차이가 있다고 알고 있습니다.

참석자 2 24:58
몇 분까지 15분은 됐어요.


clovanote.naver.com